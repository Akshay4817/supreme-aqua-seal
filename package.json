{
  "name": "constructpro-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client"
  },
  "dependencies": {
    "nodemailer": "^8.0.4"
  }
}