{
  "name": "constructpro-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "nodemailer": "^8.0.4"
  }
}